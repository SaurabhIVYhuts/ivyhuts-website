const { createNotConfiguredAdapter } = require("./notImplemented");

module.exports = createNotConfiguredAdapter(
    "uhomes",
    "No verified public API for UHomes (Milestone 23.2 audit) — real integration requires business/partner access not yet obtained."
);
