const { createNotConfiguredAdapter } = require("./notImplemented");

module.exports = createNotConfiguredAdapter(
    "university_living",
    "No verified public API for University Living (Milestone 23.2 audit) — real integration requires business/partner access not yet obtained."
);
