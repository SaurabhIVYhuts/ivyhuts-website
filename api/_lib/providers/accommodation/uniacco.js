const { createNotConfiguredAdapter } = require("./notImplemented");

module.exports = createNotConfiguredAdapter(
    "uniacco",
    "No verified public API for UniAcco (Milestone 23.2 audit) — real integration requires business/partner access not yet obtained (partner.uniacco.com exists but no public API docs were found)."
);
