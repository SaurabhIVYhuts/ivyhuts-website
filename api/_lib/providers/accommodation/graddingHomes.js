const { createNotConfiguredAdapter } = require("./notImplemented");

module.exports = createNotConfiguredAdapter(
    "gradding_homes",
    "No verified public API or partner program found for Gradding Homes (Milestone 23.2 audit) — real integration requires business/partner access not yet obtained."
);
